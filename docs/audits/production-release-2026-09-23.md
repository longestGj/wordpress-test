# Production acceptance release — 2026-09-23

The new native WordPress site is serving `https://tio2products.com/` from the isolated `tio2products-next-prod` stack. The public Caddy proxy forwards to `next_wordpress:80`. The former WordPress and database containers remain running for rollback. The first release used image `tio2products-next:b4209ad8e28b`, corresponding to source commit `b4209ad8e28b13517f8478dd912d19a23deec7fb`. The accepted local WordPress database was migrated into the new isolated production database to preserve editor changes; it is not kept in Git.

The former site's verified database, uploads, Caddyfile, Compose file, environment, and release references are in `/opt/tio2products/backups/pre-new-site-20260923T064006Z/`. The new site's verified post-acceptance database backup is `/home/deploy/tio2products-next/backups/live-after-qa-20260923.sql`. Both backup directories are outside Git. The old stack and its volumes have not been deleted. To return public traffic to the former site, copy the backed-up Caddyfile over `/opt/tio2products/current/Caddyfile` **in place** and restart `tio2products-caddy-1`; Caddy has its admin API disabled, so `caddy reload` does not work on this installation.

## Public checks

- All 58 planned public routes return 200 with the expected `https://tio2products.com/` canonical and no former-domain or loopback link. An unknown route and the former administrator author archive return 404.
- HTTPS and `www` redirects work. The supplied Google verification file returns 200 with the exact verification text. `robots.txt` points to the live WordPress sitemap.
- The live sitemap contains exactly the 57 URLs marked `target_sitemap=YES` in `planning/SEO_MAP.csv`: no missing or extra URL. It includes the Products archive and excludes the two noindex Portuguese locale parents, the Thank You page, and the administrator user archive.
- The Home card browser check passes on desktop and mobile: desktop headings keep the cards expanded, mobile accordions toggle, the page has no horizontal overflow, and all 14 grade links resolve to their product details. A backed-up, explicit migration updated only the owned Home card block after the database clone.
- A fresh browser makes no Google statistics request before consent or after rejection. Consent loads `G-SY6PZPX0VR`; withdrawal removes GA cookies and stops requests on the next page. The actual GA4 Realtime report remains an account-side check.
- One clearly labeled synthetic Contact, quotation, document, and sample request each produced the expected browser receipt and private record. All four notification statuses were `accepted` by Gmail SMTP. The four synthetic records were then deleted by exact ID and marker; no private inquiry/request records remain. Inbox delivery remains an account-side check.

The three policy pages were approved by the user before release. The Page Specs remain `ACCEPTED` while the user completes public acceptance and account-side GSC, GA4, and mailbox checks; this record does not claim those account actions are complete.

## Main branch republication

At the user's request, the updated main branch was republished from commit `b1ee1ffd170cdc057c908232467acff301f561a2` as image `tio2products-next:b1ee1ffd170c`. The release files are in `/home/deploy/tio2products-next/releases/b1ee1ffd170cdc057c908232467acff301f561a2/`. The production database and uploads volumes were retained. Immediately before switching images, a verified database, uploads, environment and Caddyfile backup was saved in `/home/deploy/tio2products-next/backups/pre-republish-b1ee1ff-20260923T093750Z/`. The prior new-site image and release directory remain available for rollback; restoring the backup database later would require preserving any inquiries received since that backup.

The guarded Home positioning and seven-link navigation migrations both passed local and production dry runs, then applied successfully in production. A repeat run reported the Home and menus already current. The new Home was reviewed locally and publicly at 1440, 768 and 390 pixels for its H1, title, meta description, navigation order, schema graph, footer identity, 14 grade links, layout overflow and browser errors. Public checks again passed for all 58 mapped routes, 57/57 expected sitemap URLs, canonical/domain references, GA4 consent behavior, GSC file, HTTPS redirects, request-form pages and 404 routes. The mail configuration remains present; no new test inquiry emails were sent for this content-only republication.

## Home visual and Products refinement publication

On 2026-09-23 the user authorized publication of the latest Home and Products updates. Production now runs image `tio2products-next:dfa483fd3b70` from source `dfa483fd3b70ea33f47b62ed0552aae422b8636b`, in `/home/deploy/tio2products-next/releases/dfa483fd3b70ea33f47b62ed0552aae422b8636b/`. Before any production write, the database, uploads, environment, Caddyfile and previous source reference were saved and SHA-256 verified at `/home/deploy/tio2products-next/backups/pre-home-products-dfa483f-20260923T103302Z/`. The previous image `tio2products-next:b1ee1ffd170c` and release directory remain available for rollback. Preserve inquiries received since the backup before considering database restoration.

The Products update preflight found six exact baseline passages. It changed only those passages in the owned Products hub, preserving other editor text and metadata; the repeat run found zero pending changes. The first image switch was automatically rolled back because a smoke assertion searched for an outdated Home phrase. Both pages returned HTTP 200, the assertion was corrected to the current H1, and the second image switch and checks passed. The production container is healthy.

Public post-release checks passed all 58 planned routes with live canonical URLs and no old-domain or loopback references, the exact 57/57 sitemap set, Google verification file, robots, request-page availability and 404. Home passed title/H1/meta, menu order, schema, all 14 grade links and browser checks at 1440, 1024, 768, 390 and 320 pixels; the 1024px Hero image edge, mobile RFQ and 320px heading were checked. Products passed all 14 grade destinations, original selector mappings, Not Sure guidance, two process links, ItemList, four FAQs and 1440/768/390 browser checks without overflow or JavaScript errors. The existing consent-controlled GA4 behavior was retested. No new inquiry emails were sent because form code was unchanged.

The four source-verification gaps for existing M-996, M-2196, M-340 and M-2377 directory summaries remain documented in `planning/pages/PRODUCT-000.md`; publication does not resolve those factual checks or imply final user acceptance.

## Applications refinement publication

On 2026-09-23 the user authorized publication of the accepted Applications update. Production now runs image `tio2products-next:fab854a7471a` from source `fab854a7471a84301a40afd4c38259748054de3a` in `/home/deploy/tio2products-next/releases/fab854a7471a84301a40afd4c38259748054de3a/`. Before any production write, the database, uploads, environment, Caddyfile and previous source reference were saved and SHA-256 verified at `/home/deploy/tio2products-next/backups/pre-applications-fab854a-20260923T110420Z/`. The prior image `tio2products-next:dfa483fd3b70` and release directory remain available for rollback; preserve inquiries received since this backup before considering database restoration.

The owned Applications page matched all 16 exact baseline passages and both SEO baselines during a read-only production dry run. The guarded migration updated those passages, SEO title and description once; a repeat run reported no pending changes. The production container is healthy. Public checks passed the five detail-card links, 30 unchanged grade links, specialty CR-901 route, canonical, CollectionPage/BreadcrumbList/ItemList, procurement links, and 1440/768/390/320 browser layouts. No horizontal overflow or JavaScript errors occurred; the first detail CTA had a visible focus outline at 320px. Full-site checks passed all 58 mapped routes, the exact 57/57 sitemap URL set, Home cards and grade links, and consent-controlled GA4 behavior. Production URL, indexing and mail configuration are present. No new inquiry emails were sent because form code was unchanged.

## Five application details and sitewide brand publication

The user confirmed publication of every change on `main` since the previous release. Source `69aaaf846a51e74537fa7fea4735a7f6671b6b18` introduced the five application-detail refinements and TiO2Products website-brand alignment. The live database, uploads, environment, Caddyfile and prior source reference were backed up and SHA-256 verified at `/home/deploy/tio2products-next/backups/pre-main-69aaaf8-20260923T131231Z/`. A separate, unexposed WordPress and MariaDB stack restored that exact backup, ran all five guarded detail migrations followed by the 51-record guarded brand migration, and confirmed every repeat run was a no-op. The isolated validation stack was removed after production checks; its data did not mix with production.

Production dry runs confirmed the five detail targets before the image switch. The same migration order then applied successfully on the live stack. The brand migration updated 51 owned active records and changed the production site title from `TiO2 Malaysia` to `TiO2Products`; it preserved the legal operator name and editor content outside exact targets. Public checks passed five detail-page content/SEO/schema/link and 1440/768/390 layout regressions, a 58-route brand/entity contract, eight page-family logo and responsive checks, all 58 mapped canonical/domain routes, and the exact 57 sitemap URLs. The new code and content were served by image `tio2products-next:69aaaf846a51`.

## Documents grade-prefill fix

Public acceptance found that the Documents hub linked to `/request-documents/?product=M-350`. WordPress interpreted `product` as its product query variable and rendered the M-350 product page, so a buyer could not reach the document request form by that action. The regression first reproduced on the local WordPress site. Source `b86488f9fb3f0ca6fe48999ff15adfb2381cc623` changed the Hub link to `prefill.product_grade`, which the request form already accepts. After a second verified backup at `/home/deploy/tio2products-next/backups/pre-documents-fix-b86488f-20260923T132200Z/`, image `tio2products-next:b86488f9fb3f` replaced the code only; the production database and uploads were retained. Public desktop and mobile browser checks now reach Request Documents with M-350 prefilled. Home cards, Products selector, consent-controlled GA4, the 58-route domain audit, 58-route brand contract and exact 57/57 sitemap set passed again. Production URL, indexing and mail configuration remain present; no test email was sent. The previous image and both verified backups remain available for rollback, with any newer inquiries preserved before a database restore.
